/*import {describe, it} from 'node:test'
import assert from 'node:assert'*/

function main() {
	if ('') {
		console.log( 'Привет' )
	} else {
		console.log('Пока')
	}
}

main()

/*describe('if (Строка с нелем)', () => {
	
	it('main()', () => {
		assert.rejects(main(), 'Привет')
	})
	

	it('main()er', () => {
		assert.strictEqual(main(), 'Пока')
	})
})
*/
